# 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# JWT密钥（生成方法：node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"）
JWT_SECRET=在此填入您的密钥

# 数据库连接（如果是PostgreSQL）
DATABASE_URL=postgres://用户名:密码@主机:端口/数据库名

# 上传目录（可选）
UPLOAD_DIR=uploads

# 端口（可选）
PORT=3000
